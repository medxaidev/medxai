{
  "description": "Case B: Diff updates the slicing definition itself (e.g., description)",
  "baseElements": [
    { "path": "Patient", "min": 0, "max": "*" },
    { "path": "Patient.identifier", "min": 0, "max": "*", "slicing": { "discriminator": [{ "type": "value", "path": "system" }], "rules": "open", "description": "Original" } },
    { "path": "Patient.identifier", "sliceName": "MRN", "min": 0, "max": "1" },
    { "path": "Patient.name", "min": 0, "max": "*" }
  ],
  "diffElements": [
    { "path": "Patient.identifier", "slicing": { "discriminator": [{ "type": "value", "path": "system" }], "rules": "open", "description": "Updated description" } }
  ],
  "expected": {
    "hasSlicingRoot": true,
    "slicingDescription": "Updated description"
  }
}
