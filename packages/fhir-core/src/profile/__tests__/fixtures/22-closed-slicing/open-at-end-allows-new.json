{
  "description": "openAtEnd slicing: allows new slice at end",
  "baseElements": [
    { "path": "Patient", "min": 0, "max": "*" },
    { "path": "Patient.identifier", "min": 0, "max": "*", "slicing": { "discriminator": [{ "type": "value", "path": "system" }], "rules": "openAtEnd" } },
    { "path": "Patient.identifier", "sliceName": "MRN", "min": 0, "max": "1" },
    { "path": "Patient.name", "min": 0, "max": "*" }
  ],
  "diffElements": [
    { "path": "Patient.identifier", "sliceName": "SSN", "min": 0, "max": "1" }
  ],
  "expected": {
    "hasSlicingError": false,
    "sliceCount": 2,
    "sliceNames": ["MRN", "SSN"]
  }
}
