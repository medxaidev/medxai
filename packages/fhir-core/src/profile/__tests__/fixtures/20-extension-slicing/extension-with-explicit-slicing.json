{
  "description": "Extension slicing: diff provides explicit slicing definition (overrides auto)",
  "baseElements": [
    { "path": "Patient", "min": 0, "max": "*" },
    { "path": "Patient.extension", "min": 0, "max": "*" },
    { "path": "Patient.name", "min": 0, "max": "*" }
  ],
  "diffElements": [
    { "path": "Patient.extension", "slicing": { "discriminator": [{ "type": "value", "path": "url" }], "rules": "closed" } },
    { "path": "Patient.extension", "sliceName": "birthPlace", "min": 0, "max": "1" }
  ],
  "expected": {
    "slicingRules": "closed",
    "sliceCount": 1,
    "sliceNames": ["birthPlace"]
  }
}
