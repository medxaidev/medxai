{
  "description": "Inherit with children: base element and its children copied",
  "base": [
    { "path": "Patient", "min": 0, "max": "*" },
    { "path": "Patient.name", "min": 0, "max": "*" },
    { "path": "Patient.name.family", "min": 0, "max": "1" },
    { "path": "Patient.name.given", "min": 0, "max": "*" },
    { "path": "Patient.birthDate", "min": 0, "max": "1" }
  ],
  "diff": [],
  "expected": {
    "elementCount": 5,
    "paths": ["Patient", "Patient.name", "Patient.name.family", "Patient.name.given", "Patient.birthDate"]
  }
}
