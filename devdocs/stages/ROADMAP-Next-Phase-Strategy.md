# 下一阶段开发策略：稳定优先的增量推进

```yaml
document_type: strategic_analysis
version: v1.0
status: PROPOSAL
created_at: 2026-02-27
scope: Post-Stage-G development direction
evidence_base: Stage 1-4 + S1 A-F + Stage G (G1-G3) fully reviewed
```

---

## 1. 当前全景状态

### 1.1 已完成阶段总览

| 阶段 | Phase | 包 | 核心能力 | 状态 |
|------|-------|------|----------|------|
| Stage-1 | 1-6 | fhir-core | Model + Parser + Context + Profile + Validator + FHIRPath | ✅ 冻结 |
| Stage-2 | 7-13 | fhir-persistence + fhir-server | Schema/DDL + CRUD + History + Search + REST API | ✅ 冻结 |
| Stage-3 | 14-17 | fhir-persistence + fhir-server | Row Indexer + Metadata Search + _include/_revinclude + Lookup-table | ✅ 冻结 |
| Stage-4 | 18-22 | all | Chained Search + Lookup Tables + Conditional Ops + Bundle + Cache + Hardening | ✅ 冻结 |
| S1 A+B | — | fhir-persistence | 7 Platform Resources + OperationContext + Multi-tenant | ✅ 冻结 |
| S1 C | — | fhir-server | JWT + Auth Routes + Middleware + AccessPolicy + Seed | ✅ 冻结 |
| S1 D/E/F | — | all | Auth E2E + Validation Gate + Audit + Migration + Client SDK | ✅ 冻结 |
| Stage G | G1-G3 | fhir-client + fhir-persistence | Platform Regression (62) + Client Gap Analysis + Client Enhancement (38) | ✅ 完成 |

### 1.2 测试数据

| 包 | 测试数 | 文件数 | 状态 |
|------|--------|--------|------|
| fhir-core | ~2400+ | ~40+ | ✅ 全部通过 |
| fhir-persistence | 961 | 37 | ✅ 全部通过 |
| fhir-server | ~110 | ~10 | ✅ 全部通过 |
| fhir-client | 63 | 3 | ✅ 全部通过 |
| **合计** | **~3600+** | **~90** | **0 failures** |

### 1.3 架构资产

- **153** 资源类型 (146 FHIR R4 + 7 MedXAI 平台)
- **4896+** DDL 语句，与 Medplum 对齐
- **完整认证链路**: login → token → refresh → Bearer → OperationContext → project isolation
- **Schema Migration 工具**: 已实现 (Phase E2)，可安全做增量 DDL 变更
- **审计日志**: AuditEvent 自动写入 (Phase E1)

---

## 2. 分层冻结策略

### 核心原则

> **已完成的层只做 Bug Fix 和 Additive Change，绝不做 Breaking Change。**

```
┌─────────────────────────────────────────────────────┐
│  Layer 0: 绝对冻结 (Absolutely Frozen)              │
│  fhir-core: Model, Parser, Context, Profile,        │
│             Validator, FHIRPath                      │
│  → 零容忍变更。任何修改需要 ADR + 全量回归。         │
├─────────────────────────────────────────────────────┤
│  Layer 1: 结构冻结 (Structurally Frozen)            │
│  fhir-persistence: Schema, DDL, Repository, Search  │
│  → 不改表结构、不改 Repo 接口签名。                  │
│  → 允许: bug fix, 新增方法 (不改现有签名),           │
│          schema migration 新增列                     │
├─────────────────────────────────────────────────────┤
│  Layer 2: 功能稳定 (Functionally Stable)            │
│  fhir-server: Routes, Auth, Middleware              │
│  → 不改现有路由行为。                                │
│  → 允许: 新增路由 (PATCH, operations, admin),        │
│          新增 middleware hook                         │
├─────────────────────────────────────────────────────┤
│  Layer 3: 活跃开发 (Active Development)             │
│  fhir-client: Client SDK                            │
│  → 自由添加功能，不影响后端                          │
├─────────────────────────────────────────────────────┤
│  Layer 4: 新增包 (New Packages)                     │
│  零风险区域 — 新增包不可能破坏已有功能               │
│  → fhir-admin, fhir-subscriptions, fhir-terminology │
│  → 前端 UI 包                                        │
└─────────────────────────────────────────────────────┘
```

### 变更安全矩阵

| 变更类型 | Layer 0 | Layer 1 | Layer 2 | Layer 3 | Layer 4 |
|----------|---------|---------|---------|---------|---------|
| Bug fix | ⚠️ ADR | ✅ | ✅ | ✅ | ✅ |
| 新增导出类型 | ⚠️ | ✅ | ✅ | ✅ | ✅ |
| 新增方法 (不改接口) | ❌ | ✅ | ✅ | ✅ | ✅ |
| 新增路由 | — | — | ✅ | — | ✅ |
| 新增包 | — | — | — | — | ✅ |
| 改现有接口签名 | ❌ | ❌ | ❌ | ✅ | ✅ |
| 改表结构 | — | ❌ | — | — | — |
| Schema migration 新增列 | — | ✅ | — | — | — |

---

## 3. 待完成事项全盘审视

### 3.1 后端缺失的 HTTP 端点 (Layer 2 — 新增路由)

当前 fhir-server 的路由只有: POST (create), GET (read), PUT (update), DELETE, history, metadata, search, auth。

**缺失但已有 persistence 层支持的:**

| 端点 | 说明 | 后端支持 | 风险 |
|------|------|----------|------|
| `PATCH /:type/:id` | JSON Patch (RFC 6902) | ❌ 需要新增 repo 方法 | 🟡 |
| `POST /` (Bundle) | HTTP-level batch/transaction | ✅ persistence 层已有 | 🟢 |
| `GET /Patient/:id/$everything` | Patient compartment export | ✅ persistence 层已有 | 🟢 |
| `POST /:type/$validate` | Resource validation | ✅ validator 已有 | 🟢 |
| `POST /` conditional create | `If-None-Exist` header | ✅ persistence 层已有 | 🟢 |
| `PUT /:type?search` conditional update | Search-based upsert | ✅ persistence 层已有 | 🟢 |

**分析**: 除了 PATCH (需要在 persistence 层新增 `patchResource` 方法)，其余都只需在 server 层新增路由，直接调用已有的 persistence 能力。**这是最安全的推进方向之一。**

### 3.2 Client SDK 未实现功能 (Layer 3 — 自由开发)

| 功能 | 优先级 | 后端依赖 |
|------|--------|----------|
| Auto-Batch | 建议 | 需要 HTTP bundle 端点 (3.1) |
| WebSocket 订阅 | 建议 | 需要后端 Subscription 支持 |
| GraphQL | 建议 | 需要后端 GraphQL 端点 |
| PKCE Login | 建议 | 需要后端 PKCE 支持 |
| Binary/Attachment | 建议 | 需要后端 Binary 路由 |

### 3.3 平台管理功能 (Layer 2 + Layer 4)

| 功能 | 说明 | 实现位置 |
|------|------|----------|
| 创建 Project | POST /admin/projects | fhir-server 新增路由 |
| 邀请用户 | POST /admin/projects/:id/invite | fhir-server 新增路由 |
| 管理 AccessPolicy | CRUD AccessPolicy | 已有 (标准 FHIR CRUD) |
| 用户列表 | GET /admin/users | fhir-server 新增路由 |
| Client 注册 | POST /admin/clients | fhir-server 新增路由 |

### 3.4 中文本地化 (跨层 — 延后)

| 子项 | 影响层 | 复杂度 |
|------|--------|--------|
| i18n 错误消息 | Layer 0 (fhir-core) + Layer 2 (server) | 中 — 需要改动核心包 |
| 拼音搜索索引 | Layer 1 (persistence schema) | 高 — 需要 schema migration |
| 中文 Profile | Layer 0 (加载新 SD) | 中 |
| 术语服务 | Layer 4 (新包) + Layer 2 (新路由) | 高 |

**分析**: 中文本地化是唯一需要触及 Layer 0/1 的工作，风险最高，应最后做。

---

## 4. 推荐推进顺序

### 原则

1. **先做零风险/低风险的 Layer 2-4 工作** — 新增路由、新增包
2. **再做需要 Layer 1 增量变更的** — schema migration 新增列
3. **最后做需要 Layer 0 变更的** — 中文本地化
4. **每个阶段结束时做全量回归测试**

### 推荐路线图

```
Phase H: 补齐服务端 HTTP 端点 (Layer 2 — additive routes only)
   ├── H1: Bundle HTTP 端点 (POST /) — 直接调用 persistence.executeBatch
   ├── H2: $validate HTTP 端点 — 直接调用 validator
   ├── H3: $everything HTTP 端点 — 直接调用 persistence
   ├── H4: Conditional create/update HTTP 端点 — headers + persistence
   ├── H5: PATCH 端点 — 需要 persistence 新增 patchResource (最小变更)
   └── 回归门: 全量测试通过

Phase I: 平台管理端点 (Layer 2 — new admin routes)
   ├── I1: Super Admin 路由 (/admin/projects, /admin/users)
   ├── I2: Project 管理 (创建、配置、成员管理)
   ├── I3: Client 注册与管理
   └── 回归门: 全量测试通过

Phase J: Client SDK 完善 (Layer 3 — zero backend risk)
   ├── J1: Auto-Batch (依赖 H1 bundle 端点)
   ├── J2: Binary/Attachment 支持
   ├── J3: PKCE Login flow
   └── 回归门: client 测试通过

Phase K: 术语服务 (Layer 4 新包 + Layer 2 新路由)
   ├── K1: CodeSystem/ValueSet CRUD (已有标准 FHIR 路径)
   ├── K2: $expand 端点
   ├── K3: $validate-code 端点
   ├── K4: 核心术语数据加载 (行政性别、民族等)
   └── 回归门: 全量测试通过

Phase L: 中文本地化 (跨层 — 最高风险)
   ├── L1: i18n 错误消息框架 (Layer 0 改动)
   ├── L2: 拼音搜索索引 (Layer 1 schema migration)
   ├── L3: 中文 Profile (CN-Patient etc.)
   ├── L4: 中国术语集 (ICD-10-CN, GB/T 3304)
   └── 回归门: 全量测试通过 + 中文专项测试

Phase M: 实时通信 (Layer 4 新包 + Layer 2 新路由)
   ├── M1: Subscription 资源 + 路由
   ├── M2: WebSocket 端点
   ├── M3: Client SubscriptionManager
   └── 回归门: 全量测试通过

Phase N: 前端 UI (全新包 — 零后端风险)
   ├── N1: React Admin 框架
   ├── N2: 认证 UI (登录/注册)
   ├── N3: 资源浏览器
   ├── N4: 管理控制台
   └── 独立测试
```

---

## 5. 风险热力图

```
                    低变更范围 ◄─────────────────────────► 高变更范围
                    │                                        │
  高风险 ─────────┤   L (中文本地化)                        │
                    │       ↑                                │
                    │   需要改 Layer 0+1                     │
                    │                                        │
  中风险 ─────────┤       H5 (PATCH — Layer 1 微调)         │
                    │                                        │
                    │   K (术语 — 新路由+新包)               │
                    │                                        │
  低风险 ─────────┤   H1-H4 (新增路由)                     │
                    │   I (Admin routes)                     │
                    │   J (Client SDK)     M (WebSocket)     │
                    │   N (前端 UI)                          │
                    │                                        │
```

---

## 6. 各阶段回归测试策略

| Phase | 最低回归门 | 说明 |
|-------|-----------|------|
| H (HTTP 端点) | `npx vitest run` 全量 + tsc clean | 新增路由不应影响已有路由 |
| I (Admin) | 同上 + admin 专项测试 | 新增路由 + 权限检查 |
| J (Client) | client 测试 + server E2E | 不涉及后端结构 |
| K (术语) | 全量回归 | 新增路由，但涉及 CRUD 路径 |
| L (中文) | **全量回归 + DDL 验证 + schema migration 验证** | 触及 Layer 0+1，最严格门控 |
| M (WebSocket) | 全量回归 + subscription 专项 | 新增连接层 |
| N (前端) | 前端独立测试 + API E2E | 不触及后端代码 |

---

## 7. 即时建议

### 如果现在就想推进：

**最安全的下一步是 Phase H1-H4** — 补齐 HTTP 端点。原因：

1. **零结构变更** — 所有 persistence 能力已经存在，只差 HTTP 路由层
2. **直接提升功能完整度** — Bundle 端点、$validate、$everything 都是 FHIR 合规必需
3. **解锁 Client 功能** — 比如 `executeBatch` 在客户端已实现，但服务端还没有 `POST /` 端点
4. **测试容易** — 类似现有 CRUD 路由的测试模式

### 如果想最大化产出且零风险：

**Phase N (前端 UI)** — 完全独立新包，不触及任何后端代码。但前提是管理端点 (Phase I) 先就绪。

### 不建议现在做的：

- **中文本地化 (Phase L)** — 需要改 Layer 0+1，当前阶段风险过高
- **多级链式搜索** — 已在 Stage-3/4 中有意延后，目前 single-level 已满足需求

---

## 8. 总结

| 维度 | 状态 |
|------|------|
| **后端核心** | ✅ 生产级别，冻结 |
| **认证授权** | ✅ 完整链路 |
| **客户端 SDK** | ✅ 必须功能已实现 |
| **HTTP 端点完整度** | 🟡 ~70% — 缺 Bundle/PATCH/operations |
| **管理功能** | 🔴 无 — 需要 Phase I |
| **中文支持** | 🔴 无 — 延后到 Phase L |
| **术语服务** | 🔴 无 — 延后到 Phase K |
| **前端 UI** | 🔴 无 — 延后到 Phase N |

**推荐立即行动**: Phase H (补齐 HTTP 端点) → Phase I (管理端点) → 然后根据业务需求决定 J/K/L/M/N 的优先级。
